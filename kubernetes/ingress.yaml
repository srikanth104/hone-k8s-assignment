apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: home-api-ingress
  namespace: home
spec:
  ingressClassName: nginx

  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: home-api-service
                port:
                  number: 80